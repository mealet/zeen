## `core` libary
Each module of this library is being injected into the compiler, please avoid big elements to reduce binary size. <br/>
Library must contain compiler-reserved interfaces and structs with their signatures.
