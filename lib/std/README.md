## `std` library
A standard set of language toolchain based on system calls and specifications. <br/>
Made to simplify and improve the user experience.

Each module requires explicit import:
```zeen
use std.MODULE; // .../std/MODULE.zn
```
