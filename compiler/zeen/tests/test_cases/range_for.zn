fn main() {
  for (i : 0..3) {
    @println("i {}", i);
  }

  for (j : 1..=3) {
    @println("j {}", j);
  }

  for (k : ..3) {
    @println("k {}", k);
  }

  for (l : 2..2) {
    @println("l {}", l);
  }

  for (m : 0..) {
    if (m == 2) {
      break;
    };
    @println("m {}", m);
  }
}