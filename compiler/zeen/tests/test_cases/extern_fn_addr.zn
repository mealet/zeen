extern fn malloc(usize) *void;
extern fn free(*void);

let ALLOC: fn(usize) *void = malloc;

fn main() i32 {
  let ptr: *i32 = ALLOC(@as(usize, 123));
  *ptr = 42;
  @println("{}", *ptr);
  free(ptr);
  return 0;
}
