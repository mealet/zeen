enum Foo {
  a,
  b: i32,
  c: { inner: i32, hello: u32 },
}

fn main() {
  let s = Foo.c { .inner = 1, .hello = 2 };
  let r = switch (s) {
    .a => -1,
    .b(v) => v,
    .c(v) if (v.inner > 100) => 100,
    .c(v) => v.inner,
  };
  @println("r: {}", r);
}
