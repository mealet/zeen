use std.c.time;
use std.c.posix.sys.time;

fn main() i32 {
  let ts: c_Timespec = @uninit();
  ts.tv_sec = 0;
  ts.tv_nsec = 50000000;
  let tsp: *const c_Timespec = @as(*const c_Timespec, &ts);
  let r = nanosleep(tsp, nullptr);
  @println("nanosleep={}", r);
  return 0;
}
